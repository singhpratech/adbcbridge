---
name: Driver finding
about: A driver or server behaves differently from what the compatibility matrix or a note says
title: "finding: <driver or database> — <one line>"
labels: finding
---

**Driver and version** (e.g. `psqlodbc 16.00.0000`, `SQL_DRIVER_NAME`/`SQL_DRIVER_VER` if you have them):

**Database and version**:

**Driver manager and OS** (unixODBC / iODBC / Windows; Linux, macOS arm64, Windows x64):

**What the matrix or note says** (link to the row or note):

**What you see instead** (the first error, verbatim, with SQLSTATE and native code):

**Reproduction without adbcBridge, if you have one** (pyodbc, isql, a C program):

**Anything else** (connection-string keywords, server settings):
