<!-- SPDX-License-Identifier: Apache-2.0 -->
# Upstream: what this project found, and gave back

Driving 53 databases through one driver across three operating systems turns up defects
that belong to other projects. This file is the record: what was found, where it was
reported, and what is documented here but not yet filed. Each reported entry stands on
evidence that anyone can reproduce without adbcBridge in the stack; the not-yet-reported
table records what the compatibility run observed, and each item is verified the same way
before it is filed.

## Reported

| Date | Project | Report | What it is | Status |
|---|---|---|---|---|
| 2026-08-25 | unixODBC | [lurcher/unixODBC#239](https://github.com/lurcher/unixODBC/issues/239) | The driver manager overwrites its own stack and heap, and the process aborts, on the first `SQL_ERROR` from a driver whose `SQLWCHAR` is 4 bytes (`extract_diag_error_w`, `SQLWCHAR sqlstate[6]`). Reproduces on 2.3.12 and 2.3.14. Filed with a driver-independent reproduction: a 40-line fake driver compiled with `SQL_WCHART_CONVERT` triggers it on Linux, its 2-byte twin does not; plus lldb frames from two real macOS drivers and a fix suggestion. Same crash as the earlier #227 (Informix), which had been closed without a reproduction. | closed (not planned) by the maintainer on 2026-08-25, who then committed a mismatch check the same day (`a2acae7`: doubled buffers, sentinel test, message before abort) — the crash is now caught rather than silent; a follow-up with a test table and a patch (report the mismatch as a diagnostic instead of aborting; prime the sentinel on the ODBC 2 path) was posted on the closed issue |
| 2026-08-25 | OpenLink Virtuoso | [openlink/virtuoso-opensource#1469](https://github.com/openlink/virtuoso-opensource/issues/1469) | The macOS driver (Homebrew 7.2.17) is built to iODBC's 4-byte `SQLWCHAR` and nothing says so; through unixODBC every application dies on its first SQL error. Asks for a unixODBC-width build or a documented iODBC-only statement; includes the UTF-8 statement-literal question. | open |
| 2026-08-25 | Dremio / Arrow Flight SQL ODBC | [dremio/warpdrive#16](https://github.com/dremio/warpdrive/issues/16) | The Apple Silicon build 0.9.7 is iODBC-width, undocumented, and aborts under unixODBC on any SQL error (three servers); `LogEnabled=true` makes `SQLAllocHandle(ENV)` fail with `IM004` (an uncaught exception in the logger); the pkg ships `arrow-odbc.ini.orig` but no `arrow-odbc.ini`; the docs contradict themselves on Apple Silicon support. | open |
| 2026-08-28 | Firebird ODBC (OdbcFb) | [FirebirdSQL/firebird-odbc-driver#299](https://github.com/FirebirdSQL/firebird-odbc-driver/issues/299) | Column-wise parameter arrays step fixed-length C types (`SQL_C_SLONG`, `SQL_C_SBIGINT`, `SQL_C_DOUBLE`, dates, timestamps) at the `BufferLength` stride, which the specification says is ignored for those types; every row receives row 1's values and the NULL indicator lands in the wrong row, with `SQL_SUCCESS` throughout. `SQL_C_CHAR` arrays are correct. Filed with three plain-ODBC C programs; reproduces on 3.0.1 and 3.5.0-rc1; row-wise binding works around it. | open |
| 2026-08-28 | Firebird ODBC (OdbcFb) | [FirebirdSQL/firebird-odbc-driver#300](https://github.com/FirebirdSQL/firebird-odbc-driver/issues/300) | Once a NULL has been bound to a `SQL_BIGINT` parameter with a character C type (`SQL_C_DEFAULT` or `SQL_C_CHAR`), every later `SQL_C_SBIGINT` rebind of that parameter writes NULL; `INTEGER` with the same pattern is fine. Reproduces on 3.0.1 and 3.5.0-rc1. | open |
| 2026-08-28 | Firebird ODBC (OdbcFb) | [FirebirdSQL/firebird-odbc-driver#301](https://github.com/FirebirdSQL/firebird-odbc-driver/issues/301) | `SQLPrepare` and `SQLExecDirect` discard `SQL_ATTR_ROWS_FETCHED_PTR` and `SQL_ATTR_ROW_STATUS_PTR` set before them, so a block cursor never learns how many rows a fetch returned; set after them they work. Reproduces on 3.0.1 and 3.5.0-rc1. | open |
| 2026-08-28 | taos-odbc (TDengine) | [taosdata/taos-connector-odbc#63](https://github.com/taosdata/taos-connector-odbc/issues/63) | Four "not implemented yet" gaps, each with a plain-ODBC program: `SQLBindCol` to `SQL_C_TYPE_TIMESTAMP` (while `SQLGetData` converts), `SQL_TYPE_TIMESTAMP` parameters, `BOOL` parameters from any C type but `SQL_C_SBIGINT`-as-`SQL_TINYINT`, and any `DECIMAL` column failing the whole `SELECT`. taos-odbc from source at 3.3.6.13. | open |
| 2026-08-28 | Apache Doris | [apache/doris#67301](https://github.com/apache/doris/issues/67301) | Server-side prepared `INSERT` refuses a string parameter sent with MySQL type `BLOB` — what MySQL Connector/ODBC sends for every `SQL_C_WCHAR` parameter — so a Unicode ODBC client cannot insert through prepared statements: `NullPointerException` in the FE on 2.1.0 (stack trace attached), `AnalysisException: Unsupported MySQL type: BLOB` on 4.1.3. Filed with the program and both versions' output. | fix PR open — [apache/doris#67306](https://github.com/apache/doris/pull/67306) by a Doris contributor, "closes #67301" |
| 2026-08-29 | clickhouse-odbc | [ClickHouse/clickhouse-odbc#582](https://github.com/ClickHouse/clickhouse-odbc/issues/582) | Two parameter-binding defects on 1.5.5.20260810, each with a standalone C program: `SQL_NULL_DATA` is ignored whenever a value buffer is bound and an empty string is sent instead (silently `''` into `String`, the epoch into `DateTime64`, a server parse error for numeric, date, decimal and bool; `SQL_DEFAULT_PARAM` the same; a NULL value pointer works); and parameter arrays — the driver's own `SQLExecute` + `SQLMoreResults`-per-set protocol from #324 — stop after set 1 because `SQLMoreResults` executes it and answers `SQL_NO_DATA`, so a 5-set array lands 2 rows with no diagnostic. | open |
| 2026-08-29 | clickhouse-odbc | [ClickHouse/clickhouse-odbc#335 (comment)](https://github.com/ClickHouse/clickhouse-odbc/issues/335#issuecomment-5464127498) | Affected-row count, open since 2021: still 0 (not −1) for every write on 1.5.5 against 26.7.5.10; noted that the server already sends `written_rows` in `X-ClickHouse-Summary` on every HTTP response by default, which the driver could surface. | open |
| 2026-08-29 | OpenLink Virtuoso | [openlink/virtuoso-opensource#1470](https://github.com/openlink/virtuoso-opensource/issues/1470) | `virtodbc.so` (Linux, unixODBC) implements `SQL_C_WCHAR` as 4-byte `wchar_t` on both the parameter and the fetch side, not the 2-byte `SQLWCHAR` the driver manager defines: a UTF-16 `hello` stores as `??`, `héllo 🚀` as `0`, wide reads widen each UTF-8 byte — all `SQL_SUCCESS`. The Linux counterpart of #1469. | open |
| 2026-08-29 | OpenLink Virtuoso | [openlink/virtuoso-opensource#1471](https://github.com/openlink/virtuoso-opensource/issues/1471) | Any `Charset=` value but the exact literal `UTF-8` aborts the process inside `SQLDriverConnect` (`GPF: Dkbox.c:638 Double free`), including every charset Virtuoso itself lists; `Charset=UTF-8` connects but returns wide columns doubly encoded with the original byte length in the indicator. | open |
| 2026-08-29 | OpenLink Virtuoso | [openlink/virtuoso-opensource#1472](https://github.com/openlink/virtuoso-opensource/issues/1472) | `SQL_C_SBIGINT` parameters store 0 and `SQL_C_SBIGINT` fetches return indicator 0 with the buffer untouched (only text declared `SQL_NUMERIC`/`SQL_DECIMAL` converts exactly); `SQL_C_SLONG` parameters are read as 8 bytes regardless of `BufferLength`. | open |
| 2026-08-29 | OpenLink Virtuoso | [openlink/virtuoso-opensource#1473](https://github.com/openlink/virtuoso-opensource/issues/1473) | A column-wise `SQL_C_TYPE_DATE` parameter array is stepped by `SQLBindParameter`'s `ColumnSize` rather than by `sizeof(SQL_DATE_STRUCT)`: `ColumnSize` 0 inserts row 0's date into every row, other values insert garbage dates, all `SQL_SUCCESS`. | open |
| 2026-08-29 | MySQL Connector/ODBC | [mysql/mysql-connector-odbc#20](https://github.com/mysql/mysql-connector-odbc/issues/20) | `SQLDescribeCol` describes a `DATETIME(6)` column as `COLUMN_SIZE` 19 / `DECIMAL_DIGITS` 0 (values are 26 characters; a buffer sized from the description gets `01004`), and `SQLColumns` reports `COLUMN_SIZE` NULL for `date`/`datetime`; MariaDB Connector/ODBC describes the same column 26/6. Filed on the GitHub mirror (bugs.mysql.com account could not be created). A plain-ODBC probe on 2026-09-04 added the consequence and the root cause for a follow-up: `SQLGetTypeInfo` reports `datetime` as `COLUMN_SIZE` 21 / `MAXIMUM_SCALE` 0, so a client sizing its bind from the type table (pyodbc) stores `13:45:10.123456` as `13:45:10.1` — byte-identical against MySQL 8.4 and SingleStore 9.1, `NO_SSPS=1` or not. | open |
| 2026-08-29 | MySQL Connector/ODBC | [mysql/mysql-connector-odbc#21](https://github.com/mysql/mysql-connector-odbc/issues/21) | `SHOW VARIABLES`/`SHOW COLLATION` against a MySQL-wire server reporting a zero character-set width (MatrixOne 4.2.0) kill the client with `SIGFPE` in `get_column_size()` (`utility.cc`) during `SQLExecDirect`, before any fetch; the same statement against MySQL 8.4.11 is fine. gdb backtrace included. | open |
| 2026-08-29 | Apache Ignite ODBC | [apache/ignite#13537](https://github.com/apache/ignite/issues/13537) | Column-wise parameter arrays test the NULL indicator of row 0 for every row: a `SQL_NULL_DATA` below row 0 stores an empty string (`VARCHAR`) or segfaults the client inside `SQLExecute` (`BINARY`, `WriteInt8Array` handed the -1 as a length); a NULL in row 0 nulls the whole batch; row-wise binding refused. `parameter.cpp` cited, fix suggested. | open |
| 2026-08-29 | OpenSearch SQL ODBC | [opensearch-project/sql-odbc#101](https://github.com/opensearch-project/sql-odbc/issues/101) | Any character value converted to `SQL_C_TYPE_TIMESTAMP`/`SQL_C_TYPE_DATE` comes back as today's date under `SQL_SUCCESS` (`convert.c` fills an unparsed `SIMPLE_TIME` from `localtime`); a `TIMESTAMP` literal takes the same path because the result-set type map lacks `timestamp`. | open |
| 2026-08-29 | OpenSearch SQL ODBC | [opensearch-project/sql-odbc#102](https://github.com/opensearch-project/sql-odbc/issues/102) | `SQLError`/`SQLErrorW` return the same connection error on every call and never `SQL_NO_DATA`, so unixODBC's `SQLDriverConnect(W)`, which prefers `SQLError` for a failed connect's diagnostics, spins forever on any failed connect (closed port, `useSSL` mismatch, bad `auth=`); `SQLGetDiagRecW` is correct, and psqlodbc avoids it by not exporting `SQLError`. | open |
| 2026-08-29 | QuestDB | [questdb/questdb#7566](https://github.com/questdb/questdb/issues/7566) | A `BINARY` column in a simple-query result is sent with format code 1 (raw bytes) where PostgreSQL always sends text, so libpq/psycopg and psqlodbc truncate the value at its first `0x00`; the extended protocol returns it whole. Side by side with PostgreSQL 16 via `PQfformat`. | fix PR open — [questdb/questdb#7569](https://github.com/questdb/questdb/pull/7569) |
| 2026-08-29 | QuestDB | [questdb/questdb#7567](https://github.com/questdb/questdb/issues/7567) | `DEALLOCATE ALL` fails with `Cannot invoke "io.questdb.cutlass.pgwire.PGPipelineEntry.close()" because "pe" is null` — even on a fresh autocommit connection with nothing prepared; a named `DEALLOCATE` works. | fix PR open — [questdb/questdb#7568](https://github.com/questdb/questdb/pull/7568) |
| 2026-08-29 | CrateDB | [crate/crate#20085](https://github.com/crate/crate/issues/20085) | A multi-statement simple query returns each `CommandComplete` tagged with the leading text of the whole string (`BEGIN;INSERT 0`, `BEGIN;INSERT 1`) instead of per-statement tags (`BEGIN`, `INSERT 0 1`), so tag-parsing clients such as psqlodbc get no row count inside a transaction. | **fixed** — [crate/crate#20088](https://github.com/crate/crate/pull/20088) merged 2026-08-31, ships in 6.4.4 |
| 2026-08-29 | psqlodbc | [postgresql-interfaces/psqlodbc#207](https://github.com/postgresql-interfaces/psqlodbc/issues/207) | `SQLGetData`/`SQLBindCol` narrow an out-of-range `int8` to `SQL_C_SLONG` (and to `SQL_C_SSHORT`) silently — `9223372036854775807::bigint` reads as `-1` under `SQL_SUCCESS` where ODBC specifies `22003`; `pg_atol`/`pg_atoi` in `convert.c` store the low bits with no range check. Stock PostgreSQL 16. | **fixed** — [postgresql-interfaces/psqlodbc#210](https://github.com/postgresql-interfaces/psqlodbc/pull/210) by Dave Cramer, merged 2026-09-04 (`SQLGetData`/`SQLBindCol` now return `SQL_ERROR` with `22003` for an out-of-range integer conversion instead of the wrapped low bits); re-test against the merge commit pending |
| 2026-08-29 | psqlodbc | [postgresql-interfaces/psqlodbc#208](https://github.com/postgresql-interfaces/psqlodbc/issues/208) | With `Protocol=7.4-0`, `SQLFreeHandle` of a prepared statement inside a transaction still sends `SAVEPOINT _per_query_svp_;DEALLOCATE …;RELEASE`; on a server without `SAVEPOINT` (QuestDB, Materialize) the error is swallowed, the server rolls the transaction back and `SQLEndTran(SQL_COMMIT)` returns `SQL_SUCCESS` for rows that are gone. Wire capture included. | **fixed** — [postgresql-interfaces/psqlodbc#209](https://github.com/postgresql-interfaces/psqlodbc/pull/209) by Dave Cramer, merged 2026-09-02 (rollback level 0 no longer savepoint-wraps the internal `DEALLOCATE`; the tested branch commit is the merged one); QuestDB confirmed, Materialize waits on its own fix |
| 2026-09-01 | Materialize | [MaterializeInc/materialize#38605](https://github.com/MaterializeInc/materialize/discussions/38605) (bug-report discussion; their tracker) | `DEALLOCATE "Name"` does not unquote the identifier, so a statement prepared at the protocol level (`Parse`) with a mixed-case name — psqlodbc's `_PLAN0x…` — cannot be released: `26000 prepared statement "\"_PLAN0x7\"" does not exist`, and inside a transaction the failure aborts it; PostgreSQL 16 releases the same statement. Filed with a psycopg side-by-side against PostgreSQL 16. The server half of the psqlodbc#208 row-loss pair. | **fix in review** — [MaterializeInc/materialize#38606](https://github.com/MaterializeInc/materialize/pull/38606) by antiguru (Materialize), opened 2026-09-01; unquotes prepared-statement, portal and cursor names before lookup |

## Documented here, not yet reported

Each of these is recorded with its first error and the conditions in
[`COMPATIBILITY.md`](COMPATIBILITY.md) and the per-OS benchmark files; reports follow as
time allows, and a reproduction contributed by anyone is welcome.

| Project | Finding | Where recorded |
|---|---|---|
| MariaDB Connector/ODBC ≥ 3.2 (Connector/C 3.4.9) | A parameter array with a NULL `DATE` segfaults the connector; the array path also misreports MySQL's row count. adbcBridge routes those versions row by row (`no_param_arrays`). | `bench/BENCHMARKS-macos.md` batch 2 |
| MariaDB Connector/ODBC 3.2.9 | Its connect-time probe `SELECT 1 FROM DUAL WHERE @@sql_mode LIKE '%ansi_quotes%'` fails on servers without a `DUAL` table (Databend, GreptimeDB); its prepared/binary-literal path fails on Doris and StarRocks. | `COMPATIBILITY.md`, macOS column |
| MariaDB Connector/ODBC 3.2.9 (macOS arm64) | Every MySQL-wire server reads at 39–47k rows/s through it, in five languages and pyodbc alike, against 1.3–4.5M rows/s through MySQL's own connector on the same machine. | `bench/BENCHMARKS-macos.md` batches 3–4 |
| MySQL Connector/ODBC 8.4.0 (Windows) | Needs `NO_SSPS=1` against every non-MySQL server (`No data supplied for parameters in prepared statement`); reads result sets from servers without character-set session variables as 3-byte `utf8`, so astral characters come back as `???` (Databend, GreptimeDB, MatrixOne, MongoDB BI). Connector/ODBC 26.7.1 for Windows reads astral characters correctly but still needs `NO_SSPS=1` against non-MySQL servers. | `bench/BENCHMARKS-windows.md` |
| MySQL Connector/ODBC 26.7.1 (macOS arm64, iODBC build) | Writes UTF-16 code units into iODBC's 4-byte `SQLWCHAR` (surrogate pairs as two units) but does not read bound `SQL_C_WCHAR` parameters in four-byte units consistently — plain ASCII inlined as garbage. adbcBridge binds text narrow for it on a four-byte build. Only obtainable through a JavaScript download page. | `docs/TROUBLESHOOTING.md`, `bench/BENCHMARKS-macos.md` batch 4 |
| Arrow Flight SQL ODBC (Windows build) | Returns a non-BMP character with only its low 16 bits (U+1F680 → U+F680) when building UTF-16 — on literals and stored data, through pyodbc and the bridge identically (sqlflite, InfluxDB 3). | `bench/BENCHMARKS-windows.md` |
| taos-odbc (TDengine, Windows build) | An ANSI build whose iconv lacks the `CP1252 → UTF-8` table: statement text from the driver manager cannot be converted, so nothing executes. | `bench/BENCHMARKS-windows.md` |
| MariaDB Connector/ODBC 3.1.15 (Linux) | `SQLBindCol(SQL_C_CHAR)` does not NUL-terminate: a value that exactly fills the buffer returns `SQL_SUCCESS` with no terminator and no `01004`, and a shorter buffer gets the data bytes with no terminator (`SQLGetData` on the same value is correct); `SQLSetPos(SQL_POSITION, n)` answers `SQL_SUCCESS` and a following `SQLGetData` returns row 1's data for every *n*; a fractional-second `TIME` string bound `SQL_C_CHAR` → `SQL_TYPE_TIME` is refused with `22008 Datetime field overflow` even into `TIME(6)`. adbcBridge reads bound columns by their indicators and already sets `getdata_repair=false` for this driver. | `tests/compat/README.md`, MariaDB section |
| MatrixOne 4.2 | A parameter array bound into a `BIT` column aborts the server once it holds NULLs (`malloc(): unaligned fastbin chunk detected`; six sets with two NULLs is enough, while single parameters, NULL-free arrays and the same array into `TINYINT` are fine); over server-side prepared statements a bound NULL into `BIT` is stored as false (`NO_SSPS=1` stores NULL); `SQLDescribeCol` sizes a `TEXT` column at one third of its widest value's byte length; `SQLColumns` reports `DATA_TYPE` 0 for every column. adbcBridge: booleans ingested as `TINYINT`, no-declared-length columns bound at `long_bind_bytes`. — **fixed on `main`** by [matrixorigin/matrixone#27645](https://github.com/matrixorigin/matrixone/pull/27645) (2026-08-26, issue #27635) before this could be filed; verified against the 2026-08-28 nightly, the released 4.2.0 still aborts. | `COMPATIBILITY.md`, `tests/compat/README.md` |
| Arrow Flight SQL ODBC 0.9.7 (Linux x86_64) | `SQLGetFunctions` reports `SQLBindParameter` and `SQLNumParams` as supported while `SQLBindParameter` always answers `HYC00` (only `SQLDescribeParam` tells the truth); `SQL_ATTR_AUTOCOMMIT = OFF` is `HYC00`, so there is no transaction control; the `SQLColumns` crash recorded above for sqlflite and InfluxDB 3 does not reproduce against Dremio 26. | `tests/compat/README.md`, Dremio section |
| OpenLink Virtuoso (`virtodbc.dll` 7.2.17, Windows) | Advertises the SQL_GD_* extensions but `SQLSetPos(SQL_POSITION)` fails on a block cursor; and the bound-column indicator array is written at a 4-byte stride on a block cursor (read at that stride by the `ind_stride_32bit` quirk). | `bench/BENCHMARKS-windows.md`; `src/odbc_driver.c` (`ind_stride_32bit`) |
| DuckDB ODBC (Windows, Rust) | Throws a C++ exception through the FFI boundary; a Rust process cannot unwind it and aborts. | `bench/LANGUAGE_BENCHMARKS-windows.md` |
| DuckDB ODBC 1.5.5 (Linux) | `SQLGetInfo(SQL_DBMS_VER)` segfaults inside the driver; a parameter array (`SQL_ATTR_PARAMSET_SIZE` > 1) stores set 0's value in every row for every fixed-length C type (only `SQL_C_CHAR` steps), with `SQL_SUCCESS` and the right `SQL_ATTR_PARAMS_PROCESSED_PTR`; a fetch writes the full 2048-row vector into bound buffers whatever the rowset size. adbcBridge binds row by row and fetches 2048-row rowsets there. | `COMPATIBILITY.md`, `tests/compat/README.md` |
| RisingWave 3.0 | A server-side statement name is not released when the client closes the statement, so psqlodbc's `_PLAN0x<handle>` names collide on the second prepare of an allocate/prepare/execute/free loop (`XX000 Duplicated statement name`). adbcBridge sets `UseServerSidePrepare=0` there. | `COMPATIBILITY.md` |
| OceanBase CE 4.4.2 | A server-side prepared execute in which a parameter arrives as `MYSQL_TYPE_NULL` (what MySQL Connector/ODBC sends for a NULL before a value has fixed the type) is refused with `Object type error` (4001); MySQL accepts the same sequence. | `COMPATIBILITY.md` |
| ArcadeDB 26.9 (PostgreSQL wire) | A `DATETIME_MICROS` literal written with a space separator and a fraction (`'2024-02-29 13:45:10.123456'`, the form psqlodbc renders a bound timestamp in) is stored as NULL under `SQL_SUCCESS`; a NULL `BOOLEAN` arrives over the wire as `false` with a non-NULL indicator. | `tests/compat/README.md` |
| `alexbrainman/odbc` (Go, Windows) | Access violation inside `api.SQLGetDiagRec` on the first driver diagnostic, on every server but SQLite. | `bench/LANGUAGE_BENCHMARKS-windows.md` |
| psqlodbc 18.x | Sends `SHOW DateStyle` at connect, which YDB's PostgreSQL layer rejects; 16.x does not. (Arguably YDB's to implement.) | `COMPATIBILITY.md`, macOS and Windows columns |
| Firebird ODBC 3.0.1 / 3.5.0-rc1 | No macOS build; Firebird 5's sample security database ships with no SYSDBA and bootstrapping one needs an administrator. | `COMPATIBILITY.md` |
| sqliteodbc 0.99991 (Linux) | `SQLGetData(SQL_C_NUMERIC)` returns a bare `SQL_ERROR` with zero diagnostic records (the same conversion through `SQLBindCol` gives `HY003 invalid type 2`); `SQL_ATTR_PARAM_STATUS_PTR` is accepted and never written, so a caller reads uninitialised status; a `TIMESTAMP` written through a bound `SQL_C_TYPE_TIMESTAMP` is stored with three fractional digits (statement text keeps six); a second `SQLGetData` on a fully returned column hands back a suffix under `SQL_SUCCESS` instead of `SQL_NO_DATA`; `SQLRowCount` on an open `SELECT` answers 0. adbcBridge reads decimals as text and timestamps whole. | `tests/compat/README.md`, SQLite section |
| MySQL Connector/ODBC 9.4.0 (against MongoDB BI Connector) | Two segfaults with standalone reproductions: `SQLColumns` crashes in `columns_i_s()` on the NULL `NUMERIC_PRECISION` mongosqld's `information_schema` reports for a `DECIMAL` column (per column, not per table; `NO_I_S=1` is accepted and changes nothing); and the handshake crashes in the bundled libmysqlclient (`authsm_begin_plugin_auth`, `strcmp()` on a NULL plugin name) when the server's greeting names no auth plugin and `PLUGIN_DIR` is unset. adbcBridge: `no_sql_columns`, `PLUGIN_DIR=`. | `COMPATIBILITY.md`, `tests/compat/README.md` |
| Arrow Flight SQL ODBC 0.9.7 (Linux) | The `SQLColumns` segfault has a root cause: `arrow::KeyValueMetadata::Get` on a null pointer from `GetColumns_Transformer::Transform`, for any table whose Flight SQL schema carries no per-field key-value metadata — every sqlflite table, InfluxDB 3's `system`/`information_schema` tables (its `iox` tables are fine), never Dremio 26; a request spanning one such table loses the good rows with it, and three `system` tables answer `SQL_SUCCESS` with zero rows for tables that have columns. `SQLGetFunctions` also reports `SQLStatistics` as supported while the call answers `HYC00`. One-call reproduction. | `tests/compat/README.md`, Flight SQL and InfluxDB sections |
| Oracle Instant Client ODBC 23.9 (SQORA, Linux) | Changing `SQL_ATTR_ROW_ARRAY_SIZE` on an open cursor is accepted and then either segfaults on a later `SQLFetch` (`bcoReturnColData`; LOB or not, at switch points that depend on cursor depth) or silently loses rows (lowering strides the result set, 100,000 rows read back as 13,440-14,336; raising can rewind and re-deliver). `SQLGetData` on a block cursor answers HY109 although `SQL_GETDATA_EXTENSIONS` advertises `SQL_GD_BLOCK|SQL_GD_BOUND`; `SQL_C_SBIGINT` is refused on both sides (parameters silently, reads with 07006) and `SQLGetTypeInfo(SQL_BIGINT)` is empty; an unconstrained `NUMBER` is read through a double and loses precision beyond 2^53. adbcBridge: `fixed_rowset`, `getdata_repair=false`, `bigint_param_as_string`. | `COMPATIBILITY.md`, `docs/reference/quirks.md` |
| Apache Ignite ODBC | The C++ platform layer has only `linux/` and `win/`; the Darwin build stops at `sys/sysinfo.h`. | `COMPATIBILITY.md`, macOS column |
| OpenSearch SQL ODBC | The macOS package is x86_64-only and links `/usr/lib/libiodbc`. | `COMPATIBILITY.md`, macOS column |
| openGauss (enmotech image, arm64) | The MOT engine panics at start inside Docker Desktop's VM (`thread identifiers exhausted`), with or without `enable_numa = false`. | `COMPATIBILITY.md`, macOS column |
| Spanner emulator | A read-write transaction left open by a killed client wedges the emulator (`FATAL: UNAVAILABLE` on every later connection). | `COMPATIBILITY.md`, Windows column |
| SAP HANA client ODBC 2.29 (`libodbchdb`, Linux, macOS and Windows alike) | Narrow statement text is decoded as Latin-1, not as the UTF-8 the driver manager hands over: the literal `'héllo 🚀'` is stored as eleven characters, so a literal never matches the same value sent as a bound `SQL_C_WCHAR` parameter; a `SQL_C_CHAR` parameter is decoded the same way; eleven locale and connection-keyword variations change nothing (unknown keywords are silently accepted), and `SQLExecDirectW` and `SQL_C_WCHAR` store it exactly (adbcBridge routes statement text through the W entry points, `wide_sql`). `SQLGetTypeInfo` returns all 33 types in alphabetical `TYPE_NAME` order with `DATA_TYPE` unsorted, against the ODBC ordering contract, which puts `SECONDDATE` before `TIMESTAMP` and `BINTEXT` before `NCLOB`, so type-table-driven DDL silently drops fractional seconds. Both reproduced without adbcBridge on 2026-09-04 (C, isql, pyodbc). | `COMPATIBILITY.md` (SAP HANA row) |
| Exasol ODBC 25.2 / 26.2 (`exaodbc`, Linux and Windows) | `SQLBindParameter` refuses `SQL_C_BINARY` for any target (`HY003`), so no binary value can reach the server; a NULL parameter described `SQL_DECIMAL` — every integer column, since `INT`/`BIGINT` are `DECIMAL` aliases — cannot be bound with `SQL_C_DEFAULT` (`SI002 C-Type not supported`, `HY010`); `SQL_GETDATA_EXTENSIONS` claims `SQL_GD_BLOCK`, but on a block rowset only the first `SQLGetData` after each `SQLFetch` succeeds — every later one, on any row `SQLSetPos(SQL_POSITION)` selects, answers `SQL_NO_DATA` with no diagnostic record — while `SQL_FORWARD_ONLY_CURSOR_ATTRIBUTES1` is `0x0` and `SQLSetPos` still returns `SQL_SUCCESS`; `SQLGetData(SQL_C_BINARY)` is also refused (`07006`). All three reproduced without adbcBridge on 2026-09-04, in C against unixODBC. | `COMPATIBILITY.md` (Exasol row) |
| Altibase ODBC 7.3 (`libaltibase_odbc`) | A `SQL_C_WCHAR` parameter above the BMP is stored as CESU-8 (a surrogate pair encoded as two 3-byte sequences) while the narrow path stores UTF-8, so the two paths disagree on the same value; `SQLGetTypeInfo` has no `SQL_LONGVARCHAR` and reports `CREATE_PARAMS` "precision" for `VARCHAR`, so a type-table-driven `VARCHAR` DDL comes out bare, which Altibase reads as `VARCHAR(1)`; BYTE/VARBYTE/BLOB carry vendor `SQL_DATA_TYPE` codes (20001, 20003, 30) and fall through to text as hex in a generic reader. Vendor's GitHub carries no source since the 2023 withdrawal of the open-source edition; support channel only. | `COMPATIBILITY.md` (Altibase row) |
| Kinetica ODBC 7.1.9 (Linux, `libKineticaODBC`) | Executes parameter *N* with the value bound at *N+1* and sends the last as NULL, under `SQL_SUCCESS` with an empty diagnostic queue (a one-parameter `INSERT` stores NULL; eight parameters fail `HY000 (1040) Driver Error: type: %d`); `SQLDescribeParam` reports every parameter `SQL_VARCHAR(1024)`; a string parameter on `SELECT ?` segfaults the process; a `DECIMAL(18,4)` column is described three ways (`SQLDescribeCol` 38/0, `SQLColumns` 18/18, `SQLGetTypeInfo` 38). Reproduced through unixODBC with no pyodbc. adbcBridge marks the entry read-only. Server side: a provably false predicate is constant-folded onto `SYSTEM.ITER`, which cannot project a `BYTES` column, so `WHERE 1=0` fails on any table holding binary (`LIMIT 0` does not). | `COMPATIBILITY.md` (Kinetica row) |
| Kinetica ODBC 7.1.9 (Windows, `KineticaODBC.dll`, [kineticadb/kinetica-client-win-odbc](https://github.com/kineticadb/kinetica-client-win-odbc)) | Exports the W entry points but transcodes W statement text through the ANSI code page before sending it: the literal `'héllo 🚀'` lands as cp1252 bytes and every later read of the row fails `HY000 (50311)` ICU conversion, identical through pyodbc; its narrow statement path passes UTF-8 through, its narrow fetch does not. | `COMPATIBILITY.md`, Windows column; `bench/BENCHMARKS-windows.md` |
| Actian Ingres ODBC 10.1 (`libiiodbcdriver`, Linux) | Its wide entry points take a four-byte `SQLWCHAR` (`typedef wchar_t SQLWCHAR`), so under unixODBC — which selects the W path by the presence of `SQLConnectW` — the connection string arrives as its first character and the connect fails `E_GC0138`; `Driver=` is copied into a 32-byte buffer with no bound, so any real path aborts the process (`*** buffer overflow detected ***` under `ConDriverInfo`); a UTF-16 surrogate pair in a `SQL_C_WCHAR` parameter is rejected (`5000B`). adbcBridge's matrix loads it behind an ANSI-only forwarding shim; a patch for both source defects is prepared and build-tested. No public source repository accepts pull requests; the report goes to Actian's community or support channel. | `COMPATIBILITY.md` (Ingres row); `tests/compat/fixtures/ingres_ansi_shim.c` |
| Actian Client 1.5 for Windows (`CAIIOD35.DLL`) | Negotiates the server's `ISO88591` with Ingres 10.1 (declaring `UTF8` is refused, `E_GC2462`) and substitutes `?` for U+1F680 on every route, pyodbc identical — arguably by design, recorded so the Windows cell's reason is on file. | `COMPATIBILITY.md`, Windows column |
| IBM i Access ODBC 1.1.0.29 (`libcwbodbc`, Linux) | The `Driver=` value is limited to 35 characters (`30119 Key value in connection string too long` at 36 — one shorter than the driver's own installed path); message catalogues and conversion tables are resolved under a compiled-in `/opt/ibm/iaccess`, without which every diagnostic degrades to `CWBNL0202 - cwbodmsg.dll`; a `CLOB` column can be neither array-bound for writing nor bound for reading, so the type its own `SQLGetTypeInfo(SQL_LONGVARCHAR)` names costs one round trip per row (8 rows/s). | `COMPATIBILITY.md` (Db2 for i row) |
| SingleStore ODBC 1.2.2 ([memsql/singlestore-odbc-connector](https://github.com/memsql/singlestore-odbc-connector)) | `SQLGetTypeInfo` fails under `sql_mode = ANSI_QUOTES` — `42S22 Unknown column 'json' in 'field list' (1054)`, the name being the first type the call would return — so the driver builds its type table from double-quoted string literals that `ANSI_QUOTES` turns into identifiers (the `SELECT "%s" AS TYPE_NAME, …` template is visible in the binary); `SQLColumns`, `SQLTables` and queries are unaffected. Separately, `SQLColumns` with a NULL catalog name segfaults the process (`strdup(NULL)` in `MADB_StmtColumnsNoInfoSchema`) whenever a column matches. Both reproduced without adbcBridge on 2026-09-04. Open-source connector with an active issue tracker; a pull request is feasible. | `COMPATIBILITY.md` (SingleStore row) |
| Docker Desktop for Windows (WSL 2.7.12) | The data VHDX never returns space to Windows while running; `wsl --shutdown` compacts it. | `bench/BENCHMARKS-windows.md` |
